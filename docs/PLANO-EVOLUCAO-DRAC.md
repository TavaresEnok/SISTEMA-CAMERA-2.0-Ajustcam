# Plano de Evolução DRAC — Ordem de Trabalho Executável

> **Status:** preparado em 2026-07-24 para execução na(s) próxima(s) sessão(ões).
> **Origem:** matriz competitiva 84/120 (DRAC vs. 13 concorrentes) → roteiro de evolução → Roteiro Mestre v2 → **verificação contra o código real** (esta passada).
> **Como usar:** execute na ordem das fases. Um item por vez. As regras da Seção 1 têm precedência sobre qualquer otimização. Marque `[x]` só com a **evidência** exigida na Seção 8. O código é a verdade; se este plano divergir do código, PARE, corrija o plano e replaneje o item.
>
> **📊 PROGRESSO (2026-07-24, não commitado — encostado p/ revisão+deploy):** **Fases 0 e 1 completas. Fase 2 essencialmente completa** (2.2, 2.3-core, 2.4, 2.5(já existia), 2.6, 2.7, 2.8, 2.9 previews, 2.10 Central→Postgres — todos feitos+verificados; falta só keystore-plaintext do 2.10). **Fase 3: mecanismo RLS PROVADO contra Postgres real (fundação); rollout gated no gatilho comercial.** **P0 da migração `SystemSetting` corrigido** (verificado nos 2 cenários). IA destravada via docker (MOG2 + object_detector rodam em container). **Suítes: api 135 · mobile 28 · web 14 · ai 47(container ML) · central 22(com PG real) — todos verdes, dentes por mutação.** Pendências reais: só o que exige **app rodando** (QA visual da paleta web, mobile no APK, integração do sprite de preview no front) ou o **gatilho comercial** (rollout dos 23 models do Tenant/RLS).
>
> **📊 RODADA 2026-07-24 (tarde) — Central = control-plane + fatias completáveis:** decisão do dono: escala/config das instalações gerenciadas **pela Central**, não código manual. **FEITOS+verificados por mim (suítes rodadas localmente):** (a) **control-plane 3.2** — `apps/central/src/datastore/compute-nodes.js` (registro de nós por instalação, inerte/retrocompatível: sem nós = single-primary) + endpoint `PATCH /api/admin/installations/:id/compute-nodes` + **18 testes** (central 36 pass/4 skip-PG); (b) **2.9 front** — sprite no scrubbing do `PlaybackPage.tsx` reusando o token do /thumbnail (web **19/19**, build ok); (c) **2.10 keystore** — senha via `-storepass:file` (fora do `ps`) no `build-client.sh` (mobile **20/20**); (d) **3.3 1ª fatia** — extração de lógica pura do `App.tsx` (1790→1772) p/ `src/utils/` (mobile **34/34**, tsc 0). Todas com **mutação provada**. **e2e RTSP (0.3) NÃO concluído** (agente caiu por limite de sessão; deixou só `scripts/e2e-rtsp-fixture.sh` parcial, não commitado). **Fronteiras honestas que permissão não remove:** 3.2 cluster real precisa de 2ª máquina existir; enforcement do 3.1/RLS precisa de cliente multi-org real.

---

## 1. Regras inegociáveis (pré-flight de todo item)

- [ ] **1.1 Licença.** Ver tabela na Seção 3. Nenhuma linha GPL entra no repo. Para itens `REIMPLEMENTAR`, protocolo clean-room: especificar o comportamento a partir da leitura, implementar a partir da especificação, **nunca transcrever**; proibido colar snippet GPL em issue/PR/commit.
- [ ] **1.2 As 5 vantagens defensáveis são INVARIANTES, não features.** Qualquer mudança que enfraqueça uma delas está errada por definição — pare e replaneje:
  - (i) câmera privada LGPD com inversão de admin — `apps/api/src/access-control/access-control.service.ts` (`canViewCamera`, `getAccessibleCameraIds`);
  - (ii) pipeline white-label multi-APK com keystore por cliente — `apps/mobile/scripts/build-client.sh`;
  - (iii) Central de provisionamento — `apps/central/src/server.js`;
  - (iv) stack de liveness mobile — `apps/mobile/src/components/WebRtcVideo.tsx`, `VideoPlayers.tsx`;
  - (v) sanitização de credencial em logs — `apps/api/src/common/security/sensitive-text.helper.ts`.
- [ ] **1.3 Fail-safe de gravação é INVARIANTE:** IA indisponível/erro ⇒ `confirmed=None` ⇒ **grava**. Nunca um subprocesso morto pode virar `confirmed=False`. Escreva o teste "IA fora ⇒ grava" **antes** de tocar o serviço de IA.
- [ ] **1.4 Produção com clientes reais.** Deploy sempre com `docker compose -f infra/docker-compose.yml -f infra/docker-compose.dev.yml ... --no-deps <serviço>` — **nunca** recriar o MediaMTX junto (derruba os paths dinâmicos de todas as câmeras). Após rebuild da API com mudança de schema: `pnpm db:migrate` (`prisma migrate deploy`). **Live saudável nunca pisca** — se piscar após deploy, é regressão (ver memória `live-stream-no-blink-invariant`).
- [ ] **1.5 Nada toca o núcleo** (gravação/streaming/IA) antes de a rede de testes daquele arquivo existir (Fase 0).
- [ ] **1.6 Definição de "pronto":** teste passando + verificação em produção com evidência (ou honestamente "não verificável aqui, requer X"). Conclusão é por **resultado medido** (SLO, fault test, latência, isolamento comprovado), nunca pela mera existência dos arquivos.
- [ ] **1.7 Ritmo.** As estimativas de "6–8 eng / 9–12 meses" da auditoria detalhada **não** são o plano. O ritmo é o destas fases, um item por vez, com evidência.

---

## 2. Fatos verificados no código (2026-07-24) — inclui correção de uma correção

> Todos os `arquivo:linha` abaixo foram lidos nesta passada, salvo onde marcado `⟲ reconfirmar`.

### 2.1 ⛔ A "correção" do worker-go no v2 estava ERRADA — o audit original estava certo
- `services/camera-worker-go/recorder.go:82-108`: o worker **continua transcodificando** para H.264 **baseline/ultrafast — com perda** (`-c:v libx264 -preset ultrafast -profile:v baseline`, linhas 101-108; o comentário 91-93 afirma "perda de qualidade a cada gravação").
- O "`-c copy` desde 2026-07-21" no comentário (linha 86) descreve o **caminho canônico da API**, citado só para **contraste** — não o worker.
- **Consequência:** o comentário/aviso em `apps/api/src/recordings/recording-process-manager.service.ts:107-119` está **CORRETO** — **NÃO editar**. O item "corrigir comentário obsoleto" (4.6 do v2) fica **cancelado**. O item de arquitetura permanece: **aposentar o caminho worker-go divergente** (Fase 2), agora com justificativa reforçada (é mesmo lossy).
- **Lição de processo:** a regra 1.6/8.2 vale inclusive para "correções" de documentos anteriores. Reconfirme no código antes de agir sobre qualquer claim herdado — inclusive os deste plano marcados `⟲`.

### 2.1-bis ✅ CORRIGIDO (2026-07-24): `migrate deploy` do zero quebrava (SystemSetting) — **fix verificado nos 2 cenários**
**Fix aplicado:** nova migração `20260501031335_add_system_setting_table` (timestamp logo após a init) com `CREATE TABLE IF NOT EXISTS "SystemSetting"`. Roda ANTES dos INSERT que quebravam (conserta install novo) e é no-op idempotente nos bancos existentes. **Verificado empiricamente em Postgres (docker), nos DOIS cenários:** (1) banco VAZIO → `migrate deploy` aplica todas as migrações e cria+popula SystemSetting (antes: falhava); (2) banco EXISTENTE (schema via db push + 34 migrações antigas baselineadas + as 2 novas pendentes) → `migrate deploy` aplica a fix out-of-order **sem erro** (o `IF NOT EXISTS` preserva a tabela existente) e cria a UserEventReview; 0 migrações falhadas. O achado original abaixo (mantido para registro):

### 2.1-bis-orig 🔴 ACHADO ORIGINAL: `prisma migrate deploy` do ZERO FALHAVA → instalação white-label nova quebrava
Rodando as migrações num Postgres limpo (docker), `migrate deploy` **falha** em `20260715150000_harden_security_and_motion_alarm` com `relation "SystemSetting" does not exist`. Causa: **nenhuma** migração cria a tabela `SystemSetting` (0 arquivos com `CREATE TABLE "SystemSetting"`; a init `20260501031334` cria só 3 tabelas), mas **2** migrações fazem `INSERT INTO "SystemSetting"` sem guarda (`20260715150000`, `20260720120000`). A tabela existe no `schema.prisma` (model `SystemSetting`) → `db push` a cria, mas o histórico de migrações não. **Impacto:** `install-drac.sh:366` usa `prisma migrate deploy` p/ instalação nova → **toda instalação nova (banco vazio) quebra**; instalações existentes seguem OK (já têm a tabela, criada por `db push` no passado). **Fix seguro (delicado — NÃO autônomo):** não dá para editar as migrações já aplicadas (checksum do Prisma quebra nos bancos existentes). Opções: (a) uma migração nova (timestamp cedo não resolve — roda depois); (b) baseline via `prisma migrate resolve` + `CREATE TABLE IF NOT EXISTS` idempotente na primeira migração que usa SystemSetting, testado nos DOIS cenários (banco vazio e banco existente); (c) reconciliar o histórico. Precisa da sua decisão — toca a integridade de migração da produção.

### 2.2 ✅ Confirmados nesta passada (âncoras exatas)
- `services/ai-service-python/main.py:157-211` — `confirm-motion` cria `cv2.VideoCapture` síncrono + até 5 `cap.read()` (timeout 5s) **dentro de handler async FastAPI** → bloqueia o event loop do uvicorn por evento ONVIF.
- `services/ai-service-python/main.py:16` — dict global `processors` mutado sem lock em `start` (142), `stop` (152), `stop_all` (272).
- `services/ai-service-python/main.py` — serviço loga com `print()`; **manter a redação de credencial** ao trocar por logging estruturado (invariante 1.2.v).
- `apps/api/src/camera-stream/mediamtx-proxy.service.ts:68,196,212` — recuperação de fleet serializada por um único booleano `recovering` (`if (this.recovering) return`).
- `infra/mediamtx.yml:30` — `webrtcAdditionalHosts: ['168.194.13.70']` **IP hardcoded** → instalação white-label nova nasce anunciando host ICE errado (fere invariante 1.2.iv).
- `apps/web/src/pages/ReviewPage.tsx:22,25,111-112` — o tipo tem `offsetSeconds`, mas a navegação envia só `occurredAt` como `?at=`; `offsetSeconds` é **ignorado** (precisão perdida no salto Revisão→playback).
- `apps/api/src/review/review.service.ts:84-85` — **N+1 admitido no próprio comentário** ("lote por câmera evitaria N+1…"): `Promise.all(rows.map(async event => <query por evento>))`.
- `apps/api/prisma/schema.prisma` — **não há entidade `Tenant`/`Organization`**; tenancy é emulada por `CameraGroup` + `CameraPermission`. (Relevante só para a Fase 3.) 34 migrações.
- `.github/workflows/ci.yml` — roda `pnpm verify` + docker build (api/web) + bundle Android com gate de permissões. **Testes via `tsx tests/*.ts` (não Jest/vitest).** `apps/api/tests/` tem só `phase2-critical.test.ts` (49 casos, bons) e `operational-e2e.test.ts` (auto-pula sem `DRAC_E2E=1`).
- **Higiene git:** rastreados → `DRAC app redesign.zip` (48K), `check_recordings.js`, `check_users.js`, `reset_admin.js`, `apps/api/reset_admin.js`, diretório `Drac-app-redesign/`. Os APKs (`drac-mobile.apk` 35M, `drac-web.apk` 4M) **não** estão rastreados (só no worktree) — apenas gitignorar explicitamente.

### 2.3 ⟲ Claims herdados (do backend audit e do v2) — RECONFIRMAR no código antes de executar
- ⟲ `services/ai-service-python/stream_processor.py:~777,792` — reconexão de captura com `time.sleep(5)` fixo, sem backoff.
- ⟲ `apps/api/src/recordings/recording-process-manager.service.ts:~1217,1238` — `getStatus` infere "gravando" pela idade do último segmento (<15min); pode reportar OK com FFmpeg morto.
- ⟲ **Chave-mestra MediaMTX** — `apps/api/src/camera-stream/camera-stream.controller.ts:~113-122`: verificar se a credencial global `MEDIAMTX_API_USER/PASS` no callback `@Public() mediamtx-auth` concede **read** a qualquer câmera, furando o streamToken. **Se confirmado, é P0** (fura o pilar LGPD em produção). Ver item 4.1.
- ⟲ `reviewedAt` **global** por evento (marcar visto afeta todos os operadores) — claim do v2; confirmar no schema/`review.service` antes do item 5.6.
- ⟲ Push processa tickets mas **não** consulta receipts do Expo — claim do v2; confirmar em `apps/api/src/notifications/` e `apps/mobile/src/services/push.ts` antes do item 5.8.

---

## 3. Tabela de licenças (fonte por item) — verificada

| Concorrente | Licença | Uso |
|---|---|---|
| Frigate | **MIT** (código; marca protegida em `TRADEMARK.md`) | **COPIÁVEL** — código funcional, **sem** nome/logo/branding; preservar notice MIT |
| Viseron | **MIT** | **COPIÁVEL** (com atribuição) |
| VibeNVR | **MIT** | **COPIÁVEL** (baixo valor) |
| Kerberos Agent | **MIT** | **COPIÁVEL** (baixo valor) |
| Scrypted | **ISC** (server) / **Apache-2.0** (ex.: `plugins/prebuffer-mixin`) — **varia por diretório** | **COPIÁVEL por diretório** — verificar o `package.json`/LICENSE do diretório específico antes de copiar |
| lightNVR, moonfire, ZoneMinder, Motion, Bluecherry | **GPL** (v2/v3) | **REIMPLEMENTAR** (clean-room; só a técnica) |
| Shinobi | Licença restritiva comercial | **REIMPLEMENTAR / evitar** |

---

## 4. FASE 0 — Fundação (fazer primeiro · ~2–3 sem · risco baixo · Δ maior)

> Não muda comportamento de produção; só adiciona rede de proteção. Destrava todas as fases seguintes.

- [~] **0.1 Framework de teste real.** ⚠️ **CORREÇÃO (regra 9.2, 2026-07-24):** o plano dizia "vitest", mas o código revelou que a casa já padronizou no **`node:test` nativo** (web e central usam `node --test`; api/mobile usavam harness `tsx` custom). vitest **não** está instalado. Decisão: adotar **`node:test`** (zero dependência nova, Node 22 que o CI já usa, consistente com web/central) e, no ai-service, **`unittest` da stdlib** (segunda correção 9.2: pytest não está instalado e o serviço não deve ganhar dependência só p/ testar).
  - [x] **api migrada** → `node --import tsx --test`. Harness custom (`test()`+`main()`) trocado por `import { test } from 'node:test'`; corpos `assert.*` intactos. Baseline preservado: **49/49 pass, exit 0**. Controle negativo (teste falho injetado) confirmou que o runner **gateia** (exit 1). `verify` da raiz agora roda `pnpm --filter api test` (glob `tests/**/*.test.ts`), então novos testes unitários passam a ser gateados no CI automaticamente.
  - [x] **convenção unit vs e2e estabelecida:** unit = `*.test.ts` (gateado sempre); e2e = `*.e2e.ts` (alvo próprio). `operational-e2e.test.ts` → renomeado `operational-e2e.e2e.ts` porque, sob o glob, seu `process.exit(0)` de skip contava como "pass" falso (teatro). O fail-when-unconfigured do e2e fica para o item 0.3.
  - [x] **mobile migrado** → `node --import tsx --test`. Importa só módulos de lógica pura (já carregavam sob node); harness custom trocado por `node:test`, helper `assert()` próprio mantido. Baseline preservado: **19/19 pass, exit 0**.
  - [x] **ai-service: scaffold `unittest` + primeiro teste real.** `services/ai-service-python/tests/` com `test_runtime_profiles.py` (**9 casos**, cobre seleção de perfil, default-para-motion, isolamento de cópia, providers ONNX). Controle negativo confirmou gating (exit 1). Job `python-tests` adicionado ao `.github/workflows/ci.yml`. ⚠️ **cv2 não existe localmente** → testes de MOG2 (`detectors/motion.py`) e `confirm-motion` (`main.py`) só rodam **dentro da imagem** — a ligar via job em container no item 0.3. Achado de passagem: `_env_float` está **duplicado** em `runtime_profiles.py:29-36` e `64-71` (limpar).
  - [ ] **web:** já usa `node --test tests/*.test.mts` — só falta cobrir mais (não migrar).
  - ⚠️ **Caveat (adição ao processo):** characterization test trava o comportamento **atual, incluindo bugs**. Ao corrigir um bug (ex.: `offsetSeconds` no item 4.3), o teste correspondente **vai ficar vermelho de propósito** — rebaselinar conscientemente **é** parte do fix, não é regressão. Documentar cada rebaseline no commit.
- [~] **0.2 Cobrir os 3 arquivos críticos hoje sem teste** — foco nos **caminhos de erro**, não só felizes:
  - **recording-process-manager.service.ts:**
    - [x] **D2 zero-perda coberto** — `apps/api/tests/recording-args.test.ts` (**9 casos**, verde). `buildArgs`/`resolveOutputVideoCodec` (puras): modo copy nunca reencoda (inclusive HEVC), transcode só quando o modo força e a fonte não casa, captura sempre MPEG-TS segmentado com o `segment_time` pedido, vídeo obrigatório + áudio opcional, transporte da câmera respeitado; + limiar de gravação estagnada. **Dentes comprovados por mutação:** `videoCodec:'copy'`→`'libx264'` no default → os 2 testes de cópia ficaram vermelhos; revertido idêntico ao HEAD.
    - [x] **disk-guard + validação de segmento cobertos** — `apps/api/tests/recording-integrity.test.ts` (**9 casos**, verde), **sem tocar produção**: sobrescrevi os seams de I/O na instância (`getStorageUsage`, `probeRecordedFileMetadata`) e testei a decisão real. Cobre: `assertMinimumStorageFree` (bloqueia início por bytes/percentual), `enforceDiskGuard` (para gravações ativas por bytes/percentual/uso≥92%; disco saudável não para nada), `registerSegment` (segmento sem duração é rejeitado → nunca vira linha no banco; válido registra com início/fim/duração corretos; nome fora do padrão ignorado). **Dentes:** `== null`→`=== undefined` na validação → o teste de rejeição ficou vermelho; revertido idêntico ao HEAD.
    - [ ] pendente: recuperação de órfãos (`recoverOrphanedSegments`), promoção do pré-buffer.
  - **mediamtx-proxy.service.ts / camera-stream:**
    - [x] **`rtsp-url.helper.ts` coberto** — `apps/api/tests/rtsp-url.test.ts` (**13 casos**, verde, puro/zero-risco). Cobre a compatibilidade de fabricante (`buildRtspUrl`: path Dahua padrão, reescrita de query Dahua, reescrita Hikvision `/Streaming/Channels/101` com subtype 0-based→`01`), **segurança de credencial** (URL-encode de `@ / :` e barra inicial), redação (`sanitizeRtspUrl`, invariante v), `isHevcCodec`, preferência de substream na grade (`resolveGridRtspProfile`), fallbacks de canal/subtype e `resolveDeliveryVideoCodec` (configurado vence; original não-HEVC entrega original; original HEVC cai para detectado). **Dentes:** off-by-one `subtype+1`→`subtype+0` no Hik → teste vermelho; revertido idêntico ao HEAD.
    - [x] **escolha de fonte + helpers de protocolo cobertos** — `apps/api/tests/live-source.test.ts` (**6 casos**, verde, via override do seam `probeStreamVideoMetadata`). Helpers puros de troca Hik↔Dahua (`alternateMainPath` main/subtype0→01, `alternateSubPath` sub/subtype1→02, `streamPixels`) + a decisão real do `chooseLiveSource`: main degradado (<720p) migra para o path alternativo de **maior resolução**; não troca por fonte pior; main saudável não custeia probes. **Dentes:** invertido `alternatePixels > primaryPixels`→`<` → os 2 testes de decisão vermelhos; revertido idêntico ao HEAD.
    - [ ] pendente: `chooseGridSource` (substream da grade), recuperação de path/watchdog (o booleano `recovering` que será substituído no item 2.2), prekill de ffmpeg; recuperação de órfãos no recording.
  - [x] **ai-service fail-safe (lado API)** — coberto em `ai-failsafe.test.ts` (ver 0.5).
  - [x] **MOG2 (`detectors/motion.py`) COBERTO via docker** — descoberto que host não tem cv2 mas **docker+rede funcionam**. `scripts/test-ai-container.sh` (imagem cacheada `Dockerfile.test` com opencv pinado) + `tests/test_motion_detector.py` (**6 casos** `skipUnless(cv2)`): warmup, detecção de objeto, **rejeição de mudança global** (dentes por mutação), cena estática, **zonas include dentro/fora** (dentes por mutação). O CI (`ci.yml` job `python-tests`) agora instala opencv e roda tudo. Isto **fecha o 0.3-container** para a IA.
- [ ] **0.3 CI que bloqueia.** Job de teste obrigatório no `.github/workflows/ci.yml`; e2e rodando de verdade contra serviço efêmero. Remover o auto-skip: **CI falha** quando o e2e obrigatório não está configurado (hoje `operational-e2e.test.ts` se auto-pula sem `DRAC_E2E=1`).
  - ⚠️ **Adição ao processo:** e2e do pipeline de vídeo só é significativo com **fonte RTSP sintética** (ex.: `ffmpeg -re -f lavfi -i testsrc → servidor RTSP efêmero`). Sem isso, o "e2e" cobre CRUD e não o caminho de streaming/gravação — teatro, não teste. Criar a fixture RTSP como parte deste item.
- [x] **0.4 Higiene do repo — APLICADO** (via `scripts/repo-hygiene.sh --apply`, sem commit). `git rm --cached` do lixo óbvio: `DRAC app redesign.zip`, `check_recordings.js`, `check_users.js`, `reset_admin.js`, `apps/api/reset_admin.js` (arquivos permanecem em disco, só saíram do índice) + entradas no `.gitignore`. Os diretórios de design/legado (`novo-design/`, `Drac-app-redesign/`, `novo-mockup-app-drac/`, `archive/`) ficam por sua escolha — rode `bash scripts/repo-hygiene.sh --apply --incluir-design` se quiser removê-los também.
- [~] **0.5 Testes-invariante da Seção 1, escritos já** (preparam Fases 2 e 3):
  - [ ] matriz adversarial de acesso (owner, convidado, membro do grupo, admin, super-admin, outro usuário) para live, gravação, snapshot, **thumbnail**, clip, export e tokens — com o caso obrigatório "**admin/super-admin recebe 403/404 para conteúdo privado alheio**" (parte já coberta em `phase2-critical.test.ts`; falta consolidar a matriz completa de derivados);
  - [x] **"IA fora ⇒ grava" (invariante 1.3) — FEITO.** `apps/api/tests/ai-failsafe.test.ts` (**8 casos**, verde). Cobre os dois lados: (A) `AiService.confirmMotion` cai para `null` quando IA desabilitada/erro/resposta não-boolean — nunca inventa `false`; (B) `OnvifEventsService.handleNativeMotion` **grava** em `null`/`true` e só suprime em `confirmed===false`. **Dentes comprovados por mutação:** troquei `=== false` por `!== true` no `onvif-events.service.ts:389` → o teste "NULL ⇒ GRAVA" ficou vermelho (exit 1); revertido idêntico ao HEAD, suite voltou a 57/57.

**Critério de saída:** CI vermelho bloqueia merge; os 3 arquivos críticos têm cobertura dos caminhos de erro.

---

## 5. FASE 1 — Quick wins verificados (~2 sem · paralelo parcial com Fase 0)

> Itens 4.1–4.3, 4.7, 4.8 **não** tocam os 3 arquivos críticos → podem andar em paralelo à Fase 0. Item 4.4/4.5 tocam núcleo → só **após** a cobertura de 0.2.

- [~] **1.1 Auditoria da "chave-mestra" MediaMTX — CONFIRMADA, mas MITIGADA (não é P0 ativo).** `camera-stream.controller.ts:103-145`: o branch da credencial global (115-122) retorna `authorized:true` para **qualquer path/ação** antes da checagem de `action`/token → concede **read+publish** em qualquer câmera. **Mitigações reais (por isso não é P0):** trade-off documentado e intencional (comentário 110-114); comparação em tempo constante (`timingSafeTextEquals`); segredo forte imposto no boot; endpoint só na rede interna do Compose + nginx 404 na borda; exige rede interna **E** a credencial. **Fix (a fazer com cuidado, NÃO autônomo):** restringir a credencial global a `publish` e forçar `read` sempre pelo token por câmera — **antes**, mapear quais fluxos internos (métricas/health/publishers) usam `read` com a credencial global, senão quebra o `read` interno legítimo. Toca o caminho de auth do live ("live nunca pisca") → merece staging e revisão do dono. Fechar `allowOrigins` por domínio do tenant é o endurecimento adjacente de menor risco.
- [x] **1.2 Portabilidade do `mediamtx.yml` — FEITO.** Diagnóstico: o `install-drac.sh:334` **já** renderiza `MEDIAMTX_WEBRTC_ADDITIONAL_HOST=$DRAC_SERVER_IP` e o compose já passava ao container mediamtx — mas faltava o **override nativo** `MTX_WEBRTCADDITIONALHOSTS` (o MediaMTX só lê `MTX_*`), então o `mediamtx.yml` com IP fixo vencia. Fix (padrão já usado nas linhas dos `MTX_HLSALLOWORIGINS`/`MTX_WEBRTCALLOWORIGINS`): adicionei `MTX_WEBRTCADDITIONALHOSTS=${MEDIAMTX_WEBRTC_ADDITIONAL_HOST:-127.0.0.1}` ao container mediamtx (`docker-compose.yml`) e troquei o IP fixo do `mediamtx.yml:30` por `127.0.0.1` (default inócuo; o override por instalação vence). **`git grep 168.194.13.70 -- infra/` = vazio** (nenhum IP de cliente versionado). YAML validado. Protege a vantagem #4 (liveness mobile) — instalação white-label nova não anuncia mais host ICE errado. ⚠️ mecanismo `MTX_*` já comprovado neste stack, mas confirmação final é num deploy.
- [x] **1.3 Precisão Revisão→playback (web) + N+1 — FEITO.**
  - **Precisão:** a causa-raiz era o `Math.round(minute)` em `PlaybackPage.tsx` (setPlayheadFromMinute e o handler do `?at=`) — o `minuteOfDay`/`hmsToMinute` já carregavam os segundos como fração. **Julgamento (alternativa melhor que o plano):** em vez de mudar a `ReviewPage` para mandar `recordingId+offsetSeconds`, removi os 2 `Math.round` — conserta o bug na RAIZ (todo caminho de navegação `?at=`, não só o da Revisão) com mudança mínima. Extraí `minuteOfDay`/`hmsToMinute` para `apps/web/src/lib/timeline-time.ts` + `apps/web/tests/timeline-time.test.mts` (**3 casos**, travam a precisão de segundo). Typecheck do web OK. ⚠️ **não verificado no app rodando** (só unit+typecheck) — precisa de conferência visual do seek + deploy.
  - **N+1:** `review.service.ts` `feed()` — substituí a `findFirst` por evento por **uma** `findMany` em lote + casamento em memória com a MESMA semântica. `apps/api/tests/review-feed.test.ts` (**2 casos**): o de correção passa antes e depois (comportamento preservado); o de contagem de queries falhava contra o código atual (documentando o N+1) e passa agora (findFirst 0, findMany 1). `tsc` OK.
- [~] **1.4 IA — trinca barata — DIFERIDO para o container (cv2), com ressalva.** Análise: (a) o **lock no dict `processors`** tem valor questionável — uvicorn roda 1 event loop e os handlers `async` não têm `await` entre o `in`-check e o set (são atômicos no loop); a race só existiria com workers>1 (processos separados, dicts separados → sem estado compartilhado) ou threads. (b) **backoff** e (c) **logging** vivem no `stream_processor.py`/`main.py` que importam `cv2` (**ausente localmente**) → não dá para rodar/testar aqui. Aplicar às cegas viola a disciplina teste-antes/mutação. Fazer no job em container do item 0.3 (onde cv2 roda), priorizando logging; o lock só se confirmar workers>1. Fonte backoff: VibeNVR `stream_reader.py` **COPIÁVEL (MIT)**. `confirm-motion` fica para 5.1.
  - [x] **backoff FEITO** (a parte pura, testável sem cv2): novo `services/ai-service-python/reconnect_backoff.py` (`compute_reconnect_delay`: exponencial 2^(n-1)·base, teto 60s, jitter ±20% p/ dessincronizar câmeras) substitui os dois `time.sleep(5)` fixos no `stream_processor.py` (usa `consecutive_capture_failures` como tentativa). `tests/test_reconnect_backoff.py` (**5 casos**, verde). Sintaxe do `stream_processor.py` validada via `ast.parse` (sem cv2). Restam lock (valor questionável) e logging estruturado — no container.
- [x] **1.5 Status de gravação derivado do fato — FEITO.** `getStatus` no ramo local com processo ativo agora verifica o **PID vivo** (`isPidAlive` via `process.kill(pid,0)`: ESRCH=morto, EPERM=vivo) antes de reportar `isRecording`/`recording_ok_local_process` — mata o "reporta OK com FFmpeg morto" (PID morto → `isRecording:false`, `stale:true`, `local_process_dead`). Novo campo `hasRecentSegment` separa "existe gravação recente" de "está gravando" nos 3 ramos. `apps/api/tests/recording-status.test.ts` (**3 casos**): "morto" e "hasRecentSegment" falhavam contra o código atual (documentando o bug), passam agora. `tsc` OK. Pendente menor: a idéia de mtime do segmento **em escrita** para o ramo órfão (API reiniciada, ffmpeg órfão) — deixei o `isRecording` do ramo sem-active-state como estava (evita falso-negativo transitório pós-restart) só expondo `hasRecentSegment`.
- [ ] **1.6 ~~Corrigir comentário do worker-go~~ — CANCELADO.** O comentário em `recording-process-manager.service.ts:107-119` está **correto** (worker é lossy, ver 2.1). **Não editar.**
- [x] **1.7 Branding runtime no web — LOGO + ACCENT feitos.** LOGO: `brandingStore` carrega `brandLogoDataUrl`; `LoginPage`/`Sidebar` renderizam o logo do cliente (fallback DRAC). CORES (accent, escopo seguro): `apps/web/src/lib/brand-colors.ts` (`hexToHslChannels` — validado contra o padrão `#0B6BD6→212 90% 44%` do index.css; `readableForegroundChannels` WCAG; `buildBrandColorCss`) com **4 testes**; o `brandingStore` injeta um `<style>` sobrescrevendo `--primary/--ring/--primary-foreground` nos temas claro/escuro **só quando o cliente define cores** (senão remove o override → DRAC intocado). **Julgamento:** apliquei só o ACCENT, não as 14 cores (backgrounds/foregrounds) — sem QA visual, mapear tudo arriscaria um painel quebrado; o accent é a cor de maior impacto e menor risco (worst case: accent trocado, app usável). Typecheck+build+test do web OK. ⚠️ conversão testada, mas o "fica bonito" precisa de conferência no app rodando.
- [x] **1.8 Push explícito no white-label — FEITO.** `app.config.js`: flag `pushEnabled` no config do cliente. `pushEnabled:true` sem `google-services.json` do pacote → **build FALHA** com mensagem clara (em vez de entregar app "com alarmes" que só faz polling). `pushEnabled:false` → desativa conscientemente. Unset → comportamento atual mas com **console.warn** visível (não mais silencioso). `node --check` OK, mobile 19/19, typecheck 0. Protege a promessa de venda (invariante 1.2.iv). Pendente menor: marcar "notificações indisponíveis" na própria UI do app (RN) — precisa de build/run do app.

---

## 6. FASE 2 — Estruturais (~6–8 sem · ordem imposta por dependências)

- [ ] **2.1 Isolar a inferência da IA.** Tirar `cv2.VideoCapture` do event loop; `confirm-motion` enfileira e responde (`asyncio.to_thread`/pool com semáforo e deadline). **Pool por MODELO/DISPOSITIVO, não processo por câmera** (senão duplica pesos e estoura RAM/VRAM). Fonte: Viseron `child_process_worker.py`/`*_subprocess.py` **COPIÁVEL (MIT)**. **Pré-req: teste fail-safe 0.5 passando antes e depois.** Alicerce do offload 3.2.
- [~] **2.2 Recuperação de fleet paralela — FEITO (parte principal).** Correção ao diagnóstico: o código já recuperava todos os paths travados por tick, mas **serialmente** (cada `await recoverStuckPath` em sequência). Troquei o booleano global `recovering` por um **Set por-path** (`recoveringPaths`) e adicionei `recoverStuckPaths()` que recupera em **paralelo com limite (4)**. `apps/api/tests/fleet-recovery.test.ts` (**2 casos**): concorrência (pico ≥2) e guard por-path. **Dentes:** `CONCURRENCY 4→1` → o teste de concorrência ficou vermelho; revertido. `tsc` OK, sem resquício do booleano. Pendente menor: trocar o `spawnSync('ffmpeg','-version')` por probe async cacheado (item separado, no recording-process-manager).
- [~] **2.3 `/metrics` Prometheus — CORE FEITO (sem dependência nova).** Em vez de `prom-client`, formatter próprio `apps/api/src/observability/prometheus.helper.ts` (puro, **4 testes**: HELP/TYPE único, escape de labels, nunca emite NaN/Inf). Endpoint `GET /metrics` (`metrics.controller.ts` + `metrics.module.ts`, registrado no `app.module`) expõe gauges **agregados** (`drac_api_up`, uptime, RSS, heap, `drac_recordings_active`) — **sem labels de câmera/IP/URL** (regra de PII/cardinalidade). `tsc` build OK (valida o wiring de DI). Pendente: métricas ricas (CPU de encode por câmera, drops, fila de IA, latência WebRTC) exigem integração + verificação em deploy; provisionar `runtime-watchdog.sh` no install; telemetria do player (lightNVR). ⚠️ o endpoint é `@Public` p/ scrape — expõe só agregados não-sensíveis.
- [ ] **2.4 Integridade de gravação + aposentar worker-go.** Comando `recordings:check` (reconciliação DB↔disco bidirecional — técnica moonfire `check.rs`/ZoneMinder `zmaudit.pl` **REIMPLEMENTAR**) + `fsync` no fechamento do segmento antes de registrar no Postgres (técnica moonfire `dir/writer.rs` **REIMPLEMENTAR**). **Worker-go:** consultar via Central se alguma instalação usa `RECORDING_CONTROL_MODE=worker`; se nenhuma, deprecar com aviso e remover `services/camera-worker-go/` num ciclo seguinte (é lossy, ver 2.1).
- [x] **2.5 Tracking de objetos — JÁ EXISTE (não era gap).** O `services/ai-service-python/detectors/object_detector.py` já usa `sv.ByteTrack` (supervision): `self._trackers: dict[str, sv.ByteTrack]` por classe, método `_track_people`, config `track_buffer`/`persistent_track_id` no `GENERAL_PROFILE`. A persistência entre frames que o item pedia já está implementada — como o worker-go, o plano superestimou o gap. (Cobertura de teste do object_detector segue pendente — precisa da stack de ML no container.)
- [x] **2.6 Revisão por usuário — FEITO.** Confirmado o "global" (`markSeen`/`feed`/`unseenCount` usavam `cameraEvent.reviewedAt`). Novo model Prisma `UserEventReview(userId, eventId, reviewedAt)` + relações em `User`/`CameraEvent` + migração `20260724120000_user_event_review`. `feed` (visto por-usuário via batch `userEventReview.findMany` + filtro `userReviews: { none: { userId } }`), `markSeen` (upsert/deleteMany por usuário), `unseenCount` (idem). `reviewedAt` global mantido como legado (não removido). `apps/api/tests/review-per-user.test.ts` (**2 casos**): userA marca → userB continua vendo NÃO-revisado (o cerne do bug). `prisma generate` + `tsc` OK. ⚠️ migração é SQL declarativo, revisável — **não aplicada** (precisa de banco); rode `prisma migrate deploy` no deploy.
- [ ] **2.7 Revisão no mobile.** Nova `apps/mobile/src/screens/ReviewScreen.tsx` consumindo `/review/feed` com rótulos pessoa/carro e clique→instante exato (reusa 1.3), badge de não-vistos, deep link. **Respeitar o gate de câmera privada do backend** (invariante 1.2.i).
- [~] **2.8 Entrega de push comprovada — CORE FEITO.** Confirmado o gap: o `push.service.ts` só tratava tickets (estágio 1), nunca os receipts (estágio 2). Novo `expo-receipts.helper.ts` (puro: `shouldRemoveToken` — só `DeviceNotRegistered` mata o token, config/rate não; `invalidTokensFromReceipts`) com **4 testes**. `sendToTokens` agora coleta `receiptIds` e usa o classificador; novo método `fetchReceipts()` consulta `getReceipts` do Expo e remove os tokens mortos que só aparecem no processamento assíncrono. `tsc` OK, callers intactos. Pendente: **agendar** o `fetchReceipts` ~15min após o envio (job bullmq com delay) — precisa da fila + verificação em runtime.
- [x] **2.9 Scrubbing de timeline com previews — FEITO (backend).** Confirmado que era gap real (existia só prévia de hover de 1 frame + thumbnail por gravação, não o scrubbing varrido). Reimplementada a técnica do Frigate do zero (GPL → só a ideia): `apps/api/src/recordings/helpers/timeline-preview.helper.ts` (puro: plano de intervalo/grid, args ffmpeg num passe único, mapa tempo→tile) + serviço (geração sob demanda, cache em disco com rename atômico, reuso do pool de concorrência do thumbnail) + endpoints `preview-meta`/`preview-sprite` reusando **o mesmíssimo gate do /thumbnail** (invariante 1.2.i — derivado de câmera privada não vaza). **13 testes** + **ffmpeg REAL validado em container** (ffprobe do sprite = 960×90 = exatamente o plano). 2 mutações provadas. **Front FEITO (2026-07-24 tarde):** `PlaybackPage.tsx` mostra o tile do sprite no hover da timeline (helper puro `apps/web/src/lib/timeline-preview.ts` espelha o backend byte-a-byte; **5 testes**, mutação Math.floor→ceil provada; fallback gracioso p/ miniatura em 404 — playback nunca quebra), reusando o **mesmo token do /thumbnail** (invariante 1.2.i preservado). web 19/19 + build ok. ⚠️ QA visual (alinhamento do tile) precisa do app rodando.
- [x] **2.10 Central em Postgres + keystore — FEITO (completo).** Camada de dados nova em `apps/central/src/datastore/` (schema PG idempotente, mappers puros, dual-read, pg-store, signing-backup, orquestrador por env). **Dual-read** (lê PG, cai para o JSON legado), escrita no PG, **JSON legado read-only** como janela de rollback (provado por sha256 idêntico antes/depois), **backup das identidades de assinatura ANTES** do backfill (com fsync). `pg` instalado só na central. **Default continua JSON** (sem `DRAC_CENTRAL_DATABASE_URL` → produção intocada). **22 testes; verifiquei eu mesmo os 4 de integração PG contra Postgres real → 22/22, 0 skip.** **Keystore FEITO (2026-07-24 tarde):** `build-client.sh` trocou `-storepass "$KS_PASS"` (visível em `ps aux`/`/proc/<pid>/cmdline`) por `-storepass:file "$PASS_FILE"`/`-keypass:file` (lê do arquivo 0600 sem passar pela linha de comando); guarda que falha cedo se a senha faltar (nunca gera APK sem assinatura); senha aleatória por-cliente em `~/toolchain/keystores/<slug>.pass` preservada. Teste de regressão em `mobile-critical.test.ts` (20/20), mutação provada (reintroduzi a forma vulnerável → vermelho → revert byte-idêntico). Nenhum segredo commitado no repo.

---

## 7. FASE 3 — Liderança (CONDICIONADA — não iniciar sem gatilho comercial)

- [~] **3.1 Tenant + RLS — FUNDAÇÃO PROVADA; rollout gated no gatilho comercial.** O **mecanismo** está implementado e **provado contra Postgres real**: `apps/api/prisma/rls/tenant-rls.sql` (função `drac_enable_tenant_rls`: ENABLE+FORCE RLS + política `app.current_tenant IS NULL OR "tenantId" = app.current_tenant`) + `verify-rls.sh` (exit 0): isolamento por tenant (A vê só A, B só B), **backward-compat** (sem o GUC → vê tudo, então o single-tenant atual fica 100% inalterado) e **dentes** (sem RLS → vaza os 2 tenants). Requisitos documentados: app conecta como role **não-superuser**, `SET LOCAL app.current_tenant` por request. **Composição LGPD:** RLS é a fronteira externa (entre tenants, no banco); `canViewCamera` (câmera privada) é a interna (dentro do tenant) — não se tocam. **Gated (não feito, por decisão do plano):** o rollout — `tenantId` nos 23 models, migração com tenant default + backfill, wrapper de transação na API, JWT com tenant, SUPER_ADMIN deixando de ser bypass — só sob o gatilho (primeiro cliente multi-organização). A fundação de-risca esse rollout: o mecanismo já está provado.
- [~] **3.2 Offload horizontal de transcode/inferência — ORQUESTRADO PELA CENTRAL (decisão do dono, 2026-07-24).** **Princípio:** o cliente da instalação NÃO gerencia escala e NÃO toca em código; **a Central é o control-plane** (já rastreia todas as instalações, provisiona, faz remote-install por SSH e recebe heartbeat — `apps/central/src/server.js`). O registro/atribuição de nós é **dado gerenciado pela Central**, não config manual por host. **Gatilho:** contrato >~100 câmeras ou multi-site. **Parar em "4.5":** nós-worker sem HA distribuída, primário como fonte única de verdade; leases/fencing só se/quando a gravação for externalizada — e **nunca antes** do ledger/integridade (2.4). Fonte: Scrypted `server/src/services/cluster-fork.ts` (**ISC**, verificar o diretório). **Fronteira física honesta:** montar o cluster real precisa de uma 2ª máquina existir (a Central provisiona quando houver) — construímos o control-plane (registro de nós por instalação + atribuição como dado, inerte/retrocompatível: instalação sem nós = comportamento single-primary atual), não um cluster fantasma. **FEITO nesta passada:** fundação do control-plane na Central (ver §6-nota).
- [~] **3.3 Refactor do `apps/mobile/App.tsx`** (god component, hoje 1772 linhas). Incremental, tela a tela, junto com features — nunca big-bang. Introduzir `react-navigation`, unificar `screens/` e `screens/redesign/`. (Dívida; não muda nota.) **1ª FATIA FEITA (2026-07-24 tarde):** extraí 3 blocos de lógica **pura** embutida (banner operacional `buildOperationalMessages` → `src/utils/operational.ts`; navegação de data `shiftDateKey` → `src/utils/format.ts`; construtores de URL de mídia `cameraPoster/recordingThumbnail/clipDownload` que estavam repetidos em 5 pontos → `src/utils/media-endpoints.ts`), com **5 testes** node:test + mutação provada; diff no App.tsx é só "remover corpo + importar", comportamento idêntico (mobile 34/34, tsc 0). Próxima fatia (SEGURA, ainda não feita): esqueleto `react-navigation` p/ desacoplar o roteamento por `tab`/`liveCamera` — exige a dependência nova + mudança de render, então fica p/ uma fatia própria. ⚠️ QA on-device pendente.

---

## 8. O que este plano deliberadamente NÃO faz

- Não persegue **120/120** como meta literal (placar é bússola, não contrato). Meta operacional: **nenhuma dimensão < 4**, liderança isolada mantida em **D5/D6/D10**, e o núcleo (D1/D2/D3) com robustez **comprovada por teste**.
- Não copia nada de lightNVR/moonfire/ZoneMinder/Motion/Bluecherry (GPL) — só técnica reimplementada em clean-room.
- Não faz 2FA por enquanto (decisão do dono do produto).
- Não adota o cronograma de programa (6–8 eng/9–12 meses) — o escopo da auditoria entra como especificação; o ritmo é o daqui.
- Não inicia a Fase 3 sem gatilho comercial.
- Não persegue iOS sem demanda contratada (D6 pode parar em "Android excelente").

---

## 9. Formato de trabalho exigido

1. **Um item por vez**, com: (a) testes escritos/rodando antes e depois; (b) deploy conforme 1.4; (c) verificação em produção com evidência; (d) commit focado por item.
2. **Código é a verdade.** Ao divergir do plano (como no worker-go, 2.1), PARE, registre a correção neste `.md` e replaneje o item. Reconfirme todo claim `⟲` no código antes de agir.
3. **Rebaseline de characterization test** ao corrigir o bug que ele fixava **é** parte do fix (0.1).
4. **Relatório curto ao fim de cada fase:** o que mudou, evidências, riscos abertos, o que da fase seguinte repriorizar.

---

## 10. Scorecard-alvo (bússola)

| Dim | Peso | Hoje | Alvo | Como se prova (resultado medido) |
|---|---|---|---|---|
| D1 Streaming | 3 | 4 | 5 | Aceite 2.2 (fleet ≤60s) + origins/credencial fechados (1.1) |
| D2 Gravação | 3 | 4 | 5 | `recordings:check` verde + status por-fato (1.5) + worker-go aposentado |
| D3 IA | 3 | 3.5 | 5 | fail-safe testado + IA isolada (2.1) + tracker (2.5) |
| D4 Playback | 2 | 3.5 | 5 | aceite 1.3 (precisão ±1s) + scrubbing (2.9) |
| D5 Multi-tenant/LGPD | 3 | 3.5 | 5 | matriz adversarial 0.5 verde + RLS (3.1, condicionado) |
| D6 Mobile | 2 | 3.5 | 5 | revisão mobile (2.7) + push comprovado (2.8/1.8) |
| D7 Operação | 2 | 3.5 | 5 | `/metrics` vivo + watchdog provisionado (2.3) |
| D8 Escala | 2 | 3 | 4.5* | offload por gatilho (3.2); *5 só com contrato ISP |
| D9 Engenharia | 2 | 2 | 5 | CI bloqueante + 3 arquivos críticos cobertos (Fase 0) |
| D10 White-label | 2 | 4 | 5 | branding web (1.7) + keystore/Central (2.10) + IP portável (1.2) |

**Sequência crítica:** Fase 0 (0.1→0.2→0.3, 0.5) destrava tudo → Fase 1 quick wins em paralelo → 2.1 (IA isolada) habilita 2.9 e 3.2 → 2.3 (métricas) habilita balanceamento de 3.2 → 3.1/3.2 só sob gatilho. Nada toca núcleo antes de 0.2.
